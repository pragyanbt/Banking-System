# This is a template file. DO NOT commit actual secrets to Git!
# Use kubectl create secret or external secret management tools

apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: banking-prod
type: Opaque
stringData:
  secret-key: "your-super-secret-jwt-key-min-256-bits-change-this-in-production"
  # IMPORTANT: Generate a secure random key for production
  # Example: openssl rand -base64 32

---
# Example of how to create secrets using kubectl:
# kubectl create secret generic mysql-secret \
#   --from-literal=root-password='YourSecurePassword123!' \
#   -n banking-prod

# kubectl create secret generic jwt-secret \
#   --from-literal=secret-key='YourSuperSecretJWTKey256Bits' \
#   -n banking-prod

